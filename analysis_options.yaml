analyzer:
  exclude:
    - build/**
include: package:flutter_lints/flutter.yaml

formatter:
  page_width: 120

linter:
  rules:
    - prefer_final_locals
    - prefer_single_quotes
    - always_declare_return_types
    - unawaited_futures
