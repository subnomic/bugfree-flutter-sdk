# Changelog

## 0.9.5

- No changes of its own: released with the server and the other SDKs

## 0.9.4

- No changes of its own: released with the server and the other SDKs

## 0.9.2

- First release: errors from `FlutterError.onError` and `PlatformDispatcher.onError`,
  manual capture, breadcrumbs for navigation, HTTP requests, the app lifecycle and
  `debugPrint`, device context, release health sessions, a feedback dialog, and
  delivery that survives rate limits and short stretches offline
